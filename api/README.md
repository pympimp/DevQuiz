install this
1. npm install --save firebase-admin@latest
2. npm install express firebase-admin firebase-functions
3. npm install --save firebase
4. npm install -g firebase-tools
พอติดตั้งเสร็จแล้วใส่คำสั่ง firebase login ใน command propmt
พอ login เสร็จใช้คำสัง่ firebase serve --only functions
5.npm install express
npm install firebase-functions ติดตั้งเพิ่มเติม
